<h1>Make a new product</h1>

<%= form_for(@product) do |f| %>
  <div>
    <%= f.label(:screen_size) %>
    <%= f.text_field(:screen_size) %>
  </div>
  <div>
    <%= f.submit('Save Product') %>
  </div>
<% end %>
